module.exports = {
    // Spreadsheet configuration
    SPREADSHEET_ID: 'YOUR_SPREADSHEET_ID', // User needs to replace this with their Google Sheet ID
    SHEET_NAME: 'Chat Logs',
    CREDENTIALS_PATH: './credentials.json' // User needs to add their Google API credentials here
};
